const open=()=>new Promise((resolve,reject)=>{const r=indexedDB.open('delib-workspace',1);r.onupgradeneeded=()=>r.result.createObjectStore('projects',{keyPath:'id'});r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);});
export async function projectStore(action,value){const db=await open();return new Promise((resolve,reject)=>{const tx=db.transaction('projects',action==='list'?'readonly':'readwrite'),store=tx.objectStore('projects');const req=action==='list'?store.getAll():action==='save'?store.put(value):store.delete(value);let result;req.onsuccess=()=>{result=req.result;};tx.oncomplete=()=>{db.close();resolve(result);};tx.onerror=()=>{db.close();reject(tx.error);};tx.onabort=()=>{db.close();reject(tx.error||new Error('Local storage unavailable'));};});}
