import {projectFromDemo,allRecords} from './workspace-core.js';
import {requestCorrection,saveSession,startProgression,advanceProgression} from './operations-core.js';
export function operationsDemo(data,lang='zh'){
 const p=projectFromDemo(data,lang),L=(a,b)=>lang==='en'?b:a,by=L('虛構主持人','Fictional facilitator'),owner=by,record=allRecords(p).find(a=>a.kind==='statement'),proposal=allRecords(p).find(a=>a.kind==='proposal');
 const base={title:L('校門現勘與方案討論','School-gate walk and options'),date:'2026-09-23',mode:'offline',owner,by,notes:L('虛構演練，非真實出席。','Fictional rehearsal, not real attendance.'),agenda:[{title:L('一起看動線','Walk the route'),minutes:20},{title:L('比較方案與限制','Compare options and constraints'),minutes:25}],groups:['A'],observations:[{participant:'demo-caregiver',group:'A',invited:true,attended:false,spoke:false,voted:false,barrier:''},{participant:'demo-shop',group:'A',invited:true,attended:false,spoke:false,voted:false,barrier:''},{participant:'demo-night-worker',group:'A',invited:true,attended:false,spoke:false,voted:false,barrier:L('夜班，改用線上表單','Night shift; offer an online form')}],evaluations:[]};
 const session=saveSession(p,null,{...base,status:'planned'});const held={...base,status:'held',observations:base.observations.map((o,i)=>({...o,attended:i<2,spoke:i===0,voted:i<2}))};saveSession(p,session.id,held);saveSession(p,session.id,{...held,status:'reviewed',evaluations:[{participant:'demo-caregiver',heard:4,understood:3,fair:4,note:L('虛構回饋：無障礙例外還不清楚','Fictional feedback: access exceptions remain unclear')}]});
 const g=startProgression(p,{proposalRef:proposal.id,rule:L('本小組至少 2 人表態、至少 2 人支持；保留反對理由。','At least two responses and two supporters in this group; retain objections.'),owner,quorum:2,minSupport:2,by});advanceProgression(p,g.id,{stage:'review',reason:L('先帶回無障礙例外與缺席者意見。','Bring back access exceptions and absent voices first.'),evidenceRefs:[record.id],eligible:3,support:1,oppose:1,abstain:0,minorityNote:L('店家擔心卸貨；先討論替代時段。','Shop deliveries need an alternative time.'),by});requestCorrection(p,record.id,{reason:L('請補充雨天接送的限制；尚未改寫原話。','Please include rainy-day access; the original remains unchanged.'),owner,due:'2026-12-01',by});return p;
}
