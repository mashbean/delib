{
  "name": "@mashbean/delib",
  "version": "0.1.0",
  "private": true,
  "description": "審議拼圖：為非工程師組合數位審議工具、資料交接與下一輪行動。",
  "type": "module",
  "bin": {
    "delib": "scripts/install-skill.mjs"
  },
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types --env production",
    "check:types": "wrangler types --check --env production",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts && vitest run --config vitest.worker.config.ts",
    "deploy:dry": "wrangler deploy --dry-run --env production",
    "check:scripts": "node --check public/app.js && node --check public/core.js && node --check public/power-ranker-core.js && node --check public/ranking-receipt-core.js && node --check public/receipt-handoff-core.js && node --check public/pocket-polis-data-core.js && node --check public/pocket-polis-receipt-core.js && node --check public/integrations/pocket-polis-data.js && node --check public/integrations/polis.js && node --check public/integrations/agora.js && node --check public/integrations/heyform.js && node --check public/integrations/tttc.js && node --check public/integrations/harmonica.js && node --check public/integrations/power-ranker.js && node --check public/results/power-ranker.js && node --check public/results/pocket-polis.js && node --check scripts/install-skill.mjs && node --check scripts/run-pocket-polis-pilot.mjs",
    "check": "npm run check:scripts && npm run check:types && npm run typecheck && npm test && npm run deploy:dry",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "install-skill": "node scripts/install-skill.mjs install-skill",
    "pilot:pocket-polis": "node scripts/run-pocket-polis-pilot.mjs"
  },
  "cloudflare": {
    "bindings": {
      "ASSETS": {
        "description": "Static site assets for the deliberation planner and results page."
      },
      "POLIS_SITE_ID": {
        "description": "Optional: paste the public Site ID generated by your [Pol.is account](https://pol.is/home). This lets your deployed Delib prepare new Pol.is conversations without asking for the ID each time. It is not a password."
      },
      "POCKET_POLIS_ORIGIN": {
        "description": "Optional Pocket Polis managed host. The public default is https://polis.mashbean.net; replace it only with a compatible deployment you operate."
      }
    }
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.2",
    "@types/node": "^26.4.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
